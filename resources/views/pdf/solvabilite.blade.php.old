<!DOCTYPE html>
<html>
<head>
    <title>Generated by ISSAT - Fiche de scolarité </title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <style>
        @page {
            margin: 5px 12px;
        }
        .page-break {
            page-break-after: always;
        }

        h4{
            padding: 0px;
            margin: 0px;
        }

        h6{
            margin: 2px;
            padding: 2px;
        }

        h6 span{
            font-size: 14px;
            color: #ffaa04;
        }

        #body td{
            font-size: 14px;
            text-align:center;
        }

        .tr-title{
            padding: 10px;
            /*border-bottom: 1px solid #000000;*/
            background: #ffe200;
            text-align: center;
        }

        .td-result{
            background: rgb(221, 222, 216);
            text-align: center;
        }

        input[type=checkbox] { display: inline; }
        input[type=checkbox]:before { font-family: DejaVu Sans; }
        #watermark {
            position: fixed;

            /** Change image dimensions**/
            /*width:    20cm;*/
            /*height:   20cm;*/

            /** Your watermark should be behind every content**/
            z-index:  -1000;
            opacity: 0.2;
            left: 6%;
        }

        .longText{
            /*transform: rotate(-90deg);*/

            /*-webkit-transform: rotate(-90deg);*/

            /*!* Firefox *!*/
            /*-moz-transform: rotate(-90deg);*/

            /*!* IE *!*/
            /*-ms-transform: rotate(-90deg);*/

            /*!* Opera *!*/
            /*-o-transform: rotate(-90deg);*/

            /*!* Internet Explorer *!*/
            /*filter: progid:DXImageTransform.Microsoft.BasicImage(rotation=3);*/
        }

        #body td, #body th{
            font-size: 14px;
            text-align: center;
            height: 30px;
            font-weight: bold;
            text-transform: uppercase;
        }

        #entete td, #entete th{
            font-size: 14px;
            text-align: center;
        }

        /*tbody td{*/
        /*font-size: 5px;*/
        /*}*/

        .bg-danger{
            background-color: rgba(255, 0, 0, 0.49);
        }
        .bg-success{
            background-color: rgba(1, 203, 114, 0.51);
        }

        #header{
            font-size: 14px;
        }

        .borderStrong{
            border:2px solid #000000;
        }


    </style>
</head>
<body>
<div id="watermark">
    <img src="assets/images/logo/issat.png" width="100%" style="margin-top: 200px"/>
</div>
<header>
    @include("pdf.entete-pv")
</header>

<hr style="background-color: #999999; border: 1px dashed #cccccc">
<h4 style="text-transform: uppercase; text-align: center">Fiche de solvabilité</h4>

<main id="body" style="padding: 30px">
    @if(count($inscription)>0)
        <table border="box" rules="all" width="100%">
            <thead class="tr-title">
                <th>Matricule</th>
                <th>Nom et prénom</th>
                <th>Debit</th>
                <th>Credit</th>
            </thead>
            <tbody>
                @foreach($inscription as $inc)
                    <tr>
                        <td>{{ $inc->dossier->matriculeDossier }}</td>
                        <td>{{ $inc->dossier->etudiant->lastname }} {{ $inc->dossier->etudiant->firstname }}</td>
                        <td>
                            @php
                                $totalV = 0;
                                foreach($inc->verssemement as $v){
                                    $total += $v->montantVerssement;
                                }
                            @endphp
                            {{ $totalV }}
                        </td>
                        <td>
                            {{ $total }}
                        </td>
                    </tr>
                @endforeach
            </tbody>
        </table>
    @endif
</main>
</body>
</html>